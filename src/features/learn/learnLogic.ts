/*import { useAppSelector } from '../../store/store'

export const learnLogic = () => {
  const card_id = useAppSelector(state => state.learn.currentCard.cardsPack_id)
  const grade = useAppSelector(state => state.learn.currentCard.grade)
  const cards = useAppSelector(state => state.cards.cards[0])
  return {
    card_id,
    grade,
    cards,
  }
}*/

export const learnLogic = () => {
  console.log(44)
}
